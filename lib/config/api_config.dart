class ApiConfig {
  // Emulador Android → 10.0.2.2 aponta para o localhost do seu PC
  // Celular físico → troque pelo IP local do seu PC (ex: 192.168.1.50)
  static const String baseUrl = "http://10.0.2.2:8000";
}