class ApiConfig {
  // Chrome / Windows Desktop → localhost funciona direto
  // Emulador Android → trocar para 10.0.2.2
  // Celular físico → trocar para o IP local do seu PC (ex: 192.168.1.50)
  static const String baseUrl = "http://10.0.2.2:8000";
}